<Window x:Class="Pangea.App.WPF.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:Pangea.App.ViewModels;assembly=Pangea.App.ViewModels" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DataContext="{d:DesignInstance Type=viewModels:ShellVM}"
        mc:Ignorable="d" WindowState="Maximized"
        Title="Shell">
    
    <!--<Window.DataContext>
        <viewModels:ShellVM />
    </Window.DataContext>-->
    
    <Grid>
        <ContentControl Content="{Binding Path=MainWindowVM}" />
    </Grid>
</Window>
